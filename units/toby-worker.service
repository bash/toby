[Unit]
Description=Toby worker
After=network.target
Requires=network.target
PropagatesReloadTo=toby-server.service

[Service]
Restart=always
ExecStart=/usr/bin/toby worker
PrivateTmp=true

[Install]
WantedBy=multi-user.target
